import { useState } from 'react'\nimport { useAuthStore } from '../../store/authStore'\nimport { FiUsers, FiHome, FiAlertCircle, FiBarChart3 } from 'react-icons/fi'\nimport { motion } from 'framer-motion'\n\nexport default function AdminDashboard() {\n  const { user } = useAuthStore()\n  const [activeTab, setActiveTab] = useState('overview')\n\n  if (user?.role !== 'admin') {\n    return (\n      <div className=\"min-h-screen flex items-center justify-center\">\n        <p>Accès refusé. Vous n'êtes pas administrateur.</p>\n      </div>\n    )\n  }\n\n  return (\n    <div className=\"min-h-screen bg-terra-light py-12\">\n      <div className=\"max-w-7xl mx-auto px-4\">\n        <h1 className=\"font-serif text-4xl font-bold text-terra-dark mb-8\">Tableau de bord Admin</h1>\n\n        <div className=\"grid md:grid-cols-4 gap-6 mb-8\">\n          {[\n            { icon: <FiHome size={24} />, label: 'Terrains', value: '1,250' },\n            { icon: <FiUsers size={24} />, label: 'Utilisateurs', value: '5,000' },\n            { icon: <FiAlertCircle size={24} />, label: 'Signalements', value: '12' },\n            { icon: <FiBarChart3 size={24} />, label: 'Revenue', value: '500K' }\n          ].map((stat, index) => (\n            <motion.div\n              key={index}\n              initial={{ opacity: 0, y: 20 }}\n              whileInView={{ opacity: 1, y: 0 }}\n              className=\"bg-white p-6 rounded-lg shadow-lg\"\n            >\n              <div className=\"text-terra-gold mb-2\">{stat.icon}</div>\n              <p className=\"text-terra-border text-sm\">{stat.label}</p>\n              <p className=\"font-bold text-2xl text-terra-dark\">{stat.value}</p>\n            </motion.div>\n          ))}\n        </div>\n\n        <div className=\"flex gap-4 mb-8 border-b-2 border-terra-border\">\n          {['overview', 'users', 'lands', 'reports'].map((tab) => (\n            <button\n              key={tab}\n              onClick={() => setActiveTab(tab)}\n              className={`py-4 px-6 font-semibold transition capitalize ${\n                activeTab === tab\n                  ? 'text-terra-gold border-b-2 border-terra-gold -mb-0.5'\n                  : 'text-terra-border'\n              }`}\n            >\n              {tab}\n            </button>\n          ))}\n        </div>\n\n        <div className=\"bg-white p-8 rounded-lg shadow-lg\">\n          {activeTab === 'overview' && (\n            <div>\n              <h2 className=\"font-bold text-2xl mb-4 text-terra-dark\">Aperçu du système</h2>\n              <p className=\"text-terra-border\">Tableau de bord statistiques</p>\n            </div>\n          )}\n          {activeTab === 'users' && (\n            <div>\n              <h2 className=\"font-bold text-2xl mb-4 text-terra-dark\">Gestion des utilisateurs</h2>\n              <p className=\"text-terra-border\">Liste des utilisateurs</p>\n            </div>\n          )}\n          {activeTab === 'lands' && (\n            <div>\n              <h2 className=\"font-bold text-2xl mb-4 text-terra-dark\">Gestion des terrains</h2>\n              <p className=\"text-terra-border\">Liste des terrains</p>\n            </div>\n          )}\n          {activeTab === 'reports' && (\n            <div>\n              <h2 className=\"font-bold text-2xl mb-4 text-terra-dark\">Signalements</h2>\n              <p className=\"text-terra-border\">Liste des signalements</p>\n            </div>\n          )}\n        </div>\n      </div>\n    </div>\n  )\n}\n